server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MarketPlace?serverTimezone=GMT-03&allowPublicKeyRetrieval=true&sslMode=DISABLED&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&createDatabaseIfNotExist=true
    username: adm
    password: AdmP4ssWord
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
